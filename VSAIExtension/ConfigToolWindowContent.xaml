<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">
  <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="14">
    <ScrollViewer.Resources>
      <Style TargetType="Label" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogLabelStyleKey}}" />
      <Style TargetType="TextBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.TextBoxStyleKey}}">
        <Setter Property="Margin" Value="0,2,0,8" />
        <Setter Property="Padding" Value="4,3" />
      </Style>
      <Style TargetType="CheckBox">
        <Setter Property="Margin" Value="0,3" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
      </Style>
      <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ComboBoxStyleKey}}">
        <Setter Property="Margin" Value="0,2,0,8" />
      </Style>
      <Style TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}">
        <Setter Property="MinWidth" Value="92" />
        <Setter Property="Margin" Value="0,0,8,0" />
        <Setter Property="Padding" Value="10,4" />
      </Style>
      <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
      </Style>
      <Style x:Key="Header" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Margin" Value="0,12,0,4" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
      </Style>
    </ScrollViewer.Resources>

    <StackPanel MinWidth="320">
      <TextBlock Text="DCA AI — Configuration" FontWeight="Bold" FontSize="15" Margin="0,0,0,2" />
      <TextBlock Text="Select one or more modes and configure the connection." Opacity="0.8" />

      <TextBlock Text="Modes" Style="{StaticResource Header}" />
      <CheckBox Content="WebSocket" IsChecked="{Binding UseWebSocket}" />
      <CheckBox Content="OpenAI / LlamaCPP" IsChecked="{Binding UseOpenAi}" />
      <CheckBox Content="Open-WebUI" IsChecked="{Binding UseOpenWebUi}" />
      <CheckBox Content="Generic REST" IsChecked="{Binding UseGenericRest}" />
      <CheckBox Content="Act as MCP Server" IsChecked="{Binding UseMcpServer}" />

      <TextBlock Text="Connection" Style="{StaticResource Header}" />
      <Label Content="Host / IP" />
      <TextBox Text="{Binding Host}" />
      <Label Content="Port" />
      <TextBox Text="{Binding Port}" />
      <CheckBox Content="Use TLS (https / wss)" IsChecked="{Binding UseTls}" />
      <Label Content="Model" />
      <TextBox Text="{Binding Model}" />

      <TextBlock Text="Authentication" Style="{StaticResource Header}" />
      <Label Content="Auth type" />
      <ComboBox SelectedIndex="{Binding AuthIndex}">
        <ComboBoxItem Content="None" />
        <ComboBoxItem Content="Bearer" />
        <ComboBoxItem Content="Session" />
        <ComboBoxItem Content="OAuth" />
        <ComboBoxItem Content="API Key Header" />
      </ComboBox>
      <Label Content="Secret / Token (stored DPAPI-encrypted)" />
      <TextBox Text="{Binding SecretText}" />

      <TextBlock Text="MCP Server" Style="{StaticResource Header}" />
      <CheckBox Content="Streamable HTTP" IsChecked="{Binding McpStreamable}" />
      <CheckBox Content="stdio" IsChecked="{Binding McpStdio}" />
      <CheckBox Content="SSE (legacy)" IsChecked="{Binding McpSse}" />
      <Label Content="MCP Port" />
      <TextBox Text="{Binding McpPort}" />

      <TextBlock Text="Editing" Style="{StaticResource Header}" />
      <CheckBox Content="Require diff approval before applying edits" IsChecked="{Binding RequireDiffApproval}" />

      <StackPanel Orientation="Horizontal" Margin="0,16,0,6">
        <Button Content="Save" Command="{Binding SaveCommand}" />
        <Button Content="Test Connection" Command="{Binding TestCommand}" />
      </StackPanel>
      <TextBlock Text="{Binding Status}" Opacity="0.85" TextWrapping="Wrap" />
    </StackPanel>
  </ScrollViewer>
</DataTemplate>
